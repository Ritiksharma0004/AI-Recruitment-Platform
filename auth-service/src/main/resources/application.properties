spring.application.name=auth-service
server.port=8081

server.error.include-message=always
server.error.include-binding-errors=always

# TiDB Cloud Serverless Connection
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://gateway01.us-east-1.prod.aws.tidbcloud.com:4000/hirenova_auth?sslMode=VERIFY_IDENTITY}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:31s3jQ423FwF9tJ.root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:1Z8LzVep5p8jC43n}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

jwt.secret=${JWT_SECRET:ThisIsMySuperSecretKeyForJwtTokenGeneration123456789012345}
jwt.expiration=${JWT_EXPIRATION:86400000}

# Resend Email Integration (HTTPS REST API - works on Render Free Tier)
resend.api.key=${RESEND_API_KEY:}
resend.from.email=${RESEND_FROM_EMAIL:HireNova AI <onboarding@resend.dev>}

# Gmail SMTP Configuration (Fallback / Local Dev)
spring.mail.host=smtp.gmail.com
spring.mail.port=465
spring.mail.username=${SPRING_MAIL_USERNAME:Ritik.sde.sharma@gmail.com}
spring.mail.password=${SPRING_MAIL_PASSWORD:mtvxatswnutweqgp}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.ssl.enable=true
spring.mail.properties.mail.smtp.ssl.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
spring.mail.properties.mail.smtp.ssl.protocols=TLSv1.2 TLSv1.3
spring.mail.properties.mail.smtp.connectiontimeout=15000
spring.mail.properties.mail.smtp.timeout=15000
spring.mail.properties.mail.smtp.writetimeout=15000
