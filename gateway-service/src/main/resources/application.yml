spring:
  application:
    name: gateway-service
  codec:
    max-in-memory-size: 25MB

server:
  port: 9090

management:
  endpoints:
    web:
      exposure:
        include: "*"


jwt:
  secret: ThisIsMySuperSecretKeyForJwtTokenGeneration123456789012345
