spring:
  application:
    name: gateway-service

server:
  port: 9090

management:
  endpoints:
    web:
      exposure:
        include: "*"


jwt:
  secret: ThisIsMySuperSecretKeyForJwtTokenGeneration123456789012345
